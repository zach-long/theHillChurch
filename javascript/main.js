//$(document).ready(function() {
  /*var viewField = $(window);
  var stickyNav = $('#stickyNav');*/

  console.log('ready');

  /*viewField.scroll(function(event) {
  	objBottom = $(stickyNav).offset().top + $(stickyNav).outerHeight();
  	winBottom = viewField.scrollTop() + viewField.height();
  	if(winBottom > objBottom && !$(stickyNav).hasClass('glued')) {
  		$(stickyNav).addClass("glued");
  	}
  });*/
//});
